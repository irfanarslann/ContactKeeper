import {
  ADD_CONTACT,
  DELETE_CONTACT,
  UPDATE_CONTACT,
  SET_CURRENT,
  CLEAR_CURRENT,
  FILTER_CONTACTS,
  CLEAR_FILTER,
} from "../ActionTypes";

const ContactReducer = (state, action) => {
  switch (action.type) {
    case "":
      break;

    default:
      break;
  }
};

export default ContactReducer;
